import { customElement, html, LitElement, css, property } from 'lit-element';

@customElement('cxl-jw-transcript')
export class CXLJWTranscriptElement extends LitElement {
  static get styles() {
    return [css``];
  }

  render() {
    return html``;
  }
}
